# Backend Environment Example
# Comma-separated origins allowed for CORS. For development, you can keep "*" but it's better to restrict to your frontend.
# Example:
# BACKEND_CORS_ALLOW_ORIGINS=http://localhost:3000,https://yourapp.com
BACKEND_CORS_ALLOW_ORIGINS=*
